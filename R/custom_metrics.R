# TODO: Create the bicor function using the methods detailed by `parallelDist`
#       and return the function pointer so that it can be used by the former.
make_bicor_metric <- function() {
  NULL
}
